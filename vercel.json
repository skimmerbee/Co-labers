{
  "version": 2,
  "builds": [
    {
      "src": "server.js", // Change to your server file name if it's different
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js" // Change to your server file name if it's different
    }
  ]
}
